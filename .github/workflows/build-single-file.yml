name: Build Launcher Bundles

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release tag (e.g. v1.0.0)"
        required: true
        type: string
  push:
    paths:
      - "index.html"
      - "src/**"
      - "scripts/build_bundle.py"
      - ".github/workflows/build-single-file.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Build debug bundle
        if: github.event_name == 'push'
        run: python scripts/build_bundle.py --out dist/index.debug.html

      - name: Build minified bundle
        if: github.event_name == 'push'
        run: python scripts/build_bundle.py --minify --out dist/index.min.html

      - name: Build release bundle (minified index.html)
        if: github.event_name == 'workflow_dispatch'
        run: python scripts/build_bundle.py --minify --out dist/index.html

      - name: Upload build artifacts
        if: github.event_name == 'push'
        uses: actions/upload-artifact@v4
        with:
          name: cbgames-offline-launcher-bundles
          path: dist/*.html

      - name: Create release and upload asset
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          name: ${{ inputs.version }}
          files: dist/index.html
